{
  "name": "cool-agent-receipts-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:backend": "tsx server.ts",
    "build": "npm --prefix frontend install && npm run build --prefix frontend && rm -rf public && cp -R frontend/dist public"
  },
  "dependencies": {
    "cool-nwc": "^3.0.0",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.5.4",
    "tsx": "^4.19.0",
    "typescript": "^5.6.2"
  }
}